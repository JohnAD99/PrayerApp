<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PrayerApp"
    xmlns:views="clr-namespace:PrayerApp.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="PrayerApp"
    x:Class="PrayerApp.AppShell">

    <FlyoutItem Title="Request prayers">
        <ShellContent
            ContentTemplate="{DataTemplate views:RequestPrayerPage}"
            Route="RequestPrayerPage" />
    </FlyoutItem>

    <FlyoutItem Title="Prayer Requests">
        <ShellContent
            ContentTemplate="{DataTemplate views:PrayerRequestsPage}"
            Route="PrayerRequestsPage" />
    </FlyoutItem>

    <FlyoutItem Title="Settings">
        <ShellContent
            ContentTemplate="{DataTemplate views:SettingsPage}"
            Route="SettingsPage" />
    </FlyoutItem>

</Shell>
